import { FeedItemCollection } from './feed-item-collection.model';

describe('FeedItemCollection', () => {
  it('should create an instance', () => {
    expect(new FeedItemCollection()).toBeTruthy();
  });
});
